# COMMAND TO CREATE A NEW PROJECT ON REACT
`npm init vite@latest`

# installing tailwindcss
`npm install -D tailwindcss@latest postcss@latest autoprefixer@latest`

`npx tailwindcss init -p`

# Start the project
`npm run dev`